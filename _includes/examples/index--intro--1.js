var owner = {};

state( owner, {
    A: state({
        // define methods, events, data, etc. here
        aMethod: function () {};
    }),

    B: state({
        aMethod: function () {};
    })
});
